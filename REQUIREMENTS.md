# VIM2 本地语音输入工具需求文档

## 1. 背景与目标

VIM2 是 VIM 第一版的后续版本。产品继续提供 Windows 全局语音输入能力，并将识别模型升级为官方 Qwen3-ASR 模型。

### 1.1 产品目标

- 用户可在任意可输入文字的 Windows 应用中，通过全局热键开始和停止录音。
- 录音过程中显示实时转写预览，停止录音后生成最终结果并自动粘贴到原输入位置。
- 支持中文、英文及中英文混合语音，优先保证识别准确率。
- 内置 Qwen3-ASR 0.6B FP16 和 Qwen3-ASR 1.7B INT8，允许用户按场景切换。
- 所有语音识别均在本机执行，不上传音频或识别文本。
- 程序、配置和模型均从 Portable 目录运行；Python 及第三方包使用系统全局环境，不在项目目录中复制虚拟环境或依赖库。
- 整个应用使用 Python 实现，通过 BAT 文件启动，不生成或依赖 EXE 程序。
- 系统托盘图标、状态配色、悬浮窗视觉风格、位置和动画沿用 VIM 第一版。

### 1.2 成功标准

- 在目标机器 NVIDIA RTX A2000 Laptop 4GB 上，两种模型均能完成识别且不发生显存不足。
- 两种模型的最终识别实时率均小于 1.0，即处理时间短于音频时长。
- 中英混合输入必须达到本文档定义的 CER、WER 和专有名词正确率门槛。
- 1.7B INT8 在中英混合、专有名词、长句或复杂语音测试集中应优于或至少不劣于 0.6B FP16。
- 用户无需安装 .NET、CUDA Toolkit、FFmpeg 或单独配置模型缓存；仅要求系统预装兼容版本的 Python、锁定版本的第三方包和满足要求的 NVIDIA 驱动。
- 正式发布必须完成本文档第 6.5 节列出的全部范围。

### 1.3 非目标

- 不提供云端识别或在线 API 回退。
- 不要求说话人分离、翻译、情绪识别或音频事件识别。
- 不要求在多个模型之间同时并行推理。
- 不在本文档中规定具体 Python UI 框架、进程通信协议或推理后端。
- 本版本不提供图形化 A/B 测试界面。
- 本版本不提供集成在正式应用内的长期性能统计或诊断日志。
- 本版本不提供模型和运行环境完整性校验。
- 本版本不提供更丰富的输入法上下文或文本格式化功能。

## 2. 用户流程

### 2.1 首次启动

1. 用户双击 Portable 目录中的 `Start.cmd`，由隐藏的 Windows PowerShell 启动 VIM2。
2. 程序验证 Python、模型、依赖和 GPU 是否可用。
3. 程序加载上次选择的模型；首次运行默认选择 Qwen3-ASR 0.6B FP16。
4. 加载期间显示明确状态，不允许用户误以为程序已经可以录音。
5. 加载成功后程序进入系统托盘，并提示全局热键已经可用。

### 2.2 语音输入

1. 用户在目标应用中将光标放在输入位置。
2. 用户按一次全局热键开始录音。
3. 状态悬浮窗显示录音状态，并约每 1 秒更新一次临时转写。
4. 用户再次按下相同热键停止录音。
5. 程序使用同一个已选模型对完整录音重新识别。
6. 最终结果替换悬浮窗中的临时预览，并自动粘贴到录音开始前的目标窗口。
7. 程序恢复待机状态。

### 2.3 模型切换

1. 用户右键单击系统托盘图标。
2. 用户从“识别模型”子菜单中选择 0.6B FP16 或 1.7B INT8。
3. 程序停止并卸载当前模型，释放 GPU 资源。
4. 程序加载新模型，并在托盘和悬浮窗中显示加载状态。
5. 新模型加载成功后恢复录音能力。
6. 用户选择应持久化，在下次启动时继续使用。

任意时刻只允许一个 ASR 模型驻留内存和显存。

只有“就绪”状态允许切换模型。录音、实时识别、最终识别期间，以及存在等待重试的音频时，模型菜单必须禁用。重试必须继续使用失败时选中的模型；用户取消录音或放弃待重试音频后才能切换。

## 3. 功能需求

### 3.1 全局热键

- 支持单键和组合键。
- 支持区分左右 Ctrl、Alt、Shift 和 Win 键。
- 默认热键沿用 VIM 第一版的 Right Alt。
- 第一次触发开始录音，第二次触发停止录音。
- 录音过程中按 Esc 取消，取消后不得识别或粘贴任何文字。
- 热键配置保存在 Portable 目录中。
- 每次完整的按下并释放只触发一次，系统按键自动重复不得重复触发。
- 通用 `Ctrl`、`Alt`、`Shift`、`Win` 匹配对应的任意左键或右键；`Left*` 和 `Right*` 只匹配指定侧。
- 组合键中的所有配置键都按下时触发；额外按下未配置的键不得导致触发。
- 热键触发后应吞掉构成该热键的按键事件，避免向目标应用输入无关字符。
- 默认 Right Alt 在完全释放后才允许发送最终结果对应的 Ctrl+V。

### 3.2 麦克风录音

- 每次开始录音时使用 Windows 当前的系统默认输入设备。
- 本版本不提供麦克风设备选择功能。
- 用户在 Windows 系统设置中切换默认麦克风后，下一次录音应使用新的默认设备，无需修改 VIM2 配置。
- 录音开始、停止和设备错误必须有明确反馈。
- 默认最长录音时间为 90 秒；达到上限后自动停止并识别。
- 不得因输入设备暂时不可用而无提示退出。

### 3.3 实时转写

- 录音期间约每 1 秒刷新一次临时文本。
- 临时文本必须由当前选中的 Qwen3-ASR 模型生成，不允许使用另一个轻量模型替代。
- 允许临时文本随上下文增加而回改。
- 临时文本必须在视觉上与已提交文本区分，不应直接反复写入目标应用。
- 实时转写失败时应继续保留录音，并在停止后尝试完整语句识别。
- 实时转写不得同时创建多个模型实例。
- 本需求只约束约 1 秒刷新、同一模型和可回改等可观察行为，不限定流式 API 或滑动窗口等实现方式。

### 3.4 最终识别

- 停止录音后，必须使用完整录音重新执行一次最终识别。
- 最终结果不得简单拼接实时片段。
- 自动检测中文、英文及中英文混合内容。
- 最终识别完成前不得向目标窗口粘贴不稳定文本。
- 空结果不得覆盖剪贴板或触发粘贴。
- 识别失败时显示错误，并允许用户重试；不得返回伪造或成功形态的空结果。
- 识别失败后应保留该次临时音频，直到重试成功、用户主动放弃或程序退出；随后立即删除。
- 用户重试时必须继续使用产生失败的模型。

### 3.5 自动粘贴

- 最终文本应粘贴到录音开始时处于前台的目标窗口。
- 粘贴行为沿用 VIM 第一版的剪贴板与 SendInput 机制。
- 程序将最终结果以 Unicode 文本写入系统剪贴板，恢复录音开始时记录的目标窗口，然后发送一次 Ctrl+V。
- 粘贴后不恢复用户原有剪贴板内容；最终识别文本保留在剪贴板中。
- 兼容常规桌面应用和远程桌面场景。
- 粘贴失败时显示明确错误，不得静默丢失识别结果。
- 即使粘贴失败，用户仍应能从界面或剪贴板取回最终文本。

### 3.6 系统托盘

托盘菜单至少包含：

- 当前状态。
- 开始/停止录音。
- 识别模型。
  - Qwen3-ASR 0.6B FP16。
  - Qwen3-ASR 1.7B INT8。
- 关于。
- 退出。

当前模型必须以选中标记显示。

托盘图标沿用 VIM 第一版的 16×16 抗锯齿圆点设计和状态颜色：

| 状态 | RGB |
|---|---|
| 加载中 | `150, 150, 150` |
| 就绪 | `60, 180, 75` |
| 录音中 | `220, 50, 50` |
| 识别中 | `230, 180, 30` |

- 圆点绘制区域为 12×12，并位于 16×16 透明画布中央。
- 托盘提示文字继续使用 VIM 第一版的 Loading、Ready、Recording 和 Processing 状态语义。
- 新增的模型切换菜单不得改变托盘图标的基本视觉风格。

### 3.7 状态与错误处理

程序至少区分以下状态：

- 启动中。
- 模型加载中。
- 就绪。
- 录音中。
- 实时识别中。
- 最终识别中。
- 模型切换中。
- 错误。

模型加载失败、CUDA 不可用、显存不足、麦克风不可用和粘贴失败必须显示可执行的错误信息。

## 4. 模型与 Portable 交付要求

### 4.1 内置模型

发布包必须预置：

| 显示名称 | 模型 | 精度 | 默认 |
|---|---|---|---|
| 快速模型 | Qwen3-ASR 0.6B | FP16 | 是 |
| 高精度模型 | Qwen3-ASR 1.7B | INT8 | 否 |

- 必须使用官方 Qwen3-ASR 权重。
- 如 INT8 由运行时动态量化，应锁定量化工具版本和参数。
- 如交付预量化权重，应记录来源、原始 revision、量化方法和校验值。
- 不得把第三方转换模型冒充为官方原始权重。
- 发布包必须附带机器可读的版本清单，锁定 Python 包、推理后端、模型 revision、量化配置及文件校验值。

当前 POC 的可复现基线为：

| 项目 | 固定值 |
|---|---|
| 0.6B 模型 ID | `Qwen/Qwen3-ASR-0.6B` |
| 0.6B revision | `5eb144179a02acc5e5ba31e748d22b0cf3e303b0` |
| 0.6B 精度 | FP16 |
| 1.7B 模型 ID | `Qwen/Qwen3-ASR-1.7B` |
| 1.7B revision | `7278e1e70fe206f11671096ffdd38061171dd6e5` |
| 1.7B 量化 | `BitsAndBytesConfig(load_in_8bit=True)` |
| qwen-asr | `0.0.6` |
| Transformers | `4.57.6` |
| PyTorch | `2.11.0+cu128` |
| bitsandbytes | `0.48.2` |

实现可以升级以上版本或更换等价 INT8 实现，但必须同步更新版本清单，并重新执行完整准确率、性能、显存和离线验收。

### 4.2 模型生命周期

- 程序启动时只加载用户选中的模型。
- 常规语音输入期间模型保持驻留，避免每次识别重复加载。
- 切换模型时必须卸载旧模型并释放 GPU 后再加载新模型。
- 若新模型加载失败，应显示错误并允许恢复到上一个可用模型。
- A/B 测试工具应继续采用独立进程方式，确保两个模型的显存测量互不影响。

### 4.3 Portable 目录

除 Python 外，所有运行内容必须位于应用目录：

```text
VIM2/
├── Start.cmd
├── start.bat
├── start.ps1
├── app/
│   └── Python 应用代码
├── requirements.lock
├── .models/
│   ├── Qwen3-ASR-0.6B/
│   └── Qwen3-ASR-1.7B-INT8/
├── config/
│   ├── settings.json
│   └── hotkey.conf
└── tools/
    └── A/B 测试工具
```

- 正常运行不得依赖用户目录中的 Hugging Face 或 ModelScope 缓存。
- 正常运行不得自动联网下载模型。
- 发布准备时必须从本机现有 Hugging Face 缓存复制模型到 `VIM2\.models`，不得重新从网络下载。
- 复制完成后必须保留原缓存文件，不得移动、重命名或删除原模型。
- 如果目标模型目录已经完整存在，不得重复复制或覆盖。
- 当前模型源目录为：
  - `C:\Users\keynesy\.cache\huggingface\hub\models--Qwen--Qwen3-ASR-0.6B`，约 1.75 GB。
  - `C:\Users\keynesy\.cache\huggingface\hub\models--Qwen--Qwen3-ASR-1.7B`，约 4.38 GB。
- 应从各模型 `snapshots\<revision>` 目录复制完整快照到对应的 Portable 模型目录；不得仅复制 Hugging Face 缓存中的单个 blob 文件。
- 移动整个 VIM2 目录后，除全局 Python 环境及其第三方包外不需要重新配置。
- 路径中包含空格或中文时必须正常运行。
- 配置、日志和模型路径均应以应用目录为基准。
- 发布说明必须列出支持的 Python 大版本、最低 NVIDIA 驱动版本和实际验证过的 Windows 版本。
- `Start.cmd` 和 `start.ps1` 必须从自身所在目录解析所有相对路径，不得依赖用户当前工作目录。
- `Start.cmd` 必须通过隐藏的 Windows PowerShell 和 `pythonw.exe` 启动，不得在桌面保留命令行窗口。
- 启动脚本必须检查 Python 是否存在且版本兼容；图形启动失败时使用消息框显示明确错误。`start.bat` 作为诊断入口保留控制台输出。
- Python 第三方依赖使用全局 Python 环境，版本由根目录 `requirements.lock` 锁定；正常启动只验证依赖，不得执行 `pip install`。
- 正式应用的业务逻辑、托盘、悬浮窗、全局热键、录音、模型管理和自动粘贴均使用 Python 实现。

### 4.4 离线与隐私

- 模型准备完成后，断网环境下必须可启动和识别。
- 不得上传音频、识别文本、设备信息或性能数据。
- 如提供更新检查，必须默认关闭或明确征得用户同意。

## 5. 界面原型

### 5.1 系统托盘菜单

```text
┌─────────────────────────────────┐
│ VIM2：就绪                      │
├─────────────────────────────────┤
│ 开始录音                        │
│ 识别模型                     ▶  │
│   ✓ 快速：Qwen3-ASR 0.6B FP16   │
│     高精度：Qwen3-ASR 1.7B INT8 │
├─────────────────────────────────┤
│ 关于                            │
│ 退出                            │
└─────────────────────────────────┘
```

### 5.2 录音悬浮窗

```text
┌──────────────────────────────────────────────┐
│ ● 正在录音  00:07                            │
│ 我们需要把 Kubernetes deployment 更新到……   │
└──────────────────────────────────────────────┘
```

- 红色状态点表示正在录音。
- 显示录音时长。
- 显示当前模型产生的临时文本。
- 临时文本长度超过窗口时显示末尾内容，但完整文本应保留在内部状态中。
- 悬浮窗不得抢占目标应用焦点。
- 悬浮窗沿用 VIM 第一版的深色半透明圆角胶囊风格：
  - 背景色 `#E0191920`。
  - 边框色 `#50505064`，边框宽度 1。
  - 圆角半径 14。
  - 文字色 `#F0F0F5`，Segoe UI 12。
  - 录音圆点颜色 `#E63C3C`，尺寸 10×10。
- 悬浮窗位于目标窗口所在显示器工作区的底部中央，距离底部 60 个逻辑像素。
- 悬浮窗保持置顶、从任务栏隐藏、鼠标穿透且永不获取焦点。
- 显示和隐藏继续使用约 200 毫秒淡入淡出。
- 录音红点继续以约 50 毫秒间隔在 0.3 到 1.0 透明度之间脉动。
- 高度保持 32 个逻辑像素；宽度可从第一版的 110 扩大以容纳实时文字，但整体仍保持单行紧凑胶囊外观。

### 5.3 最终识别状态

```text
┌──────────────────────────────────────────────┐
│ ◐ 正在生成最终结果                           │
│ Qwen3-ASR 0.6B FP16                          │
└──────────────────────────────────────────────┘
```

### 5.4 模型切换状态

```text
┌──────────────────────────────────────────────┐
│ 正在切换到高精度模型                         │
│ Qwen3-ASR 1.7B INT8                          │
└──────────────────────────────────────────────┘
```

切换期间托盘菜单不得允许重复触发模型切换或开始录音。

## 6. 性能、可靠性与验收标准

### 6.1 目标设备

最低验收设备：

```text
Windows 10/11 x64
NVIDIA RTX A2000 Laptop GPU，4GB 显存
32GB 系统内存
兼容版本 Python
```

### 6.2 性能要求

- 0.6B FP16 最终识别 RTF 必须小于 1.0。
- 1.7B INT8 最终识别 RTF 必须小于 1.0。
- 实时临时文本目标刷新间隔约为 1 秒。
- 开始录音后 2 秒内必须出现第一段非空临时文本；此后存在新语音时，更新间隔的 P95 不得超过 1.5 秒。
- 实时识别不得导致录音丢帧或 UI 卡死。
- 0.6B FP16 峰值显存不得超过 2.5 GiB。
- 1.7B INT8 峰值显存不得超过 3.8 GiB。
- 模型切换完成后，旧模型对应的 CUDA 资源必须释放。
- 性能统计必须区分模型加载时间和纯推理时间。

当前 POC 基线：

| 模型 | 音频 | 推理时间 | RTF | 峰值显存 |
|---|---:|---:|---:|---:|
| 0.6B FP16 | 15.05 秒 | 6.17 秒 | 0.410 | 1.75 GiB |
| 1.7B INT8 | 15.05 秒 | 19.33 秒 | 1.284 | 2.81 GiB |

1.7B INT8 当前尚未达到 RTF 小于 1.0 的目标，正式实现必须优化推理路径，或将该项作为发布阻塞问题明确处理。

性能验收统一采用以下方法：

- 使用固定版本的验收语料和版本清单。
- 模型预先加载并完成一次不计分的预热识别。
- 每个模型单独启动测试进程，测试前确认无其他 GPU 计算任务。
- 每条音频连续测试 3 次，记录中位数。
- 最终 RTF 按“全部纯推理时间之和 / 全部音频时长之和”计算，不包含模型加载和文件下载。
- 同时单独报告冷启动模型加载时间。
- 峰值显存以进程级 CUDA/NVML 测量为准，并记录驱动、Python、PyTorch、推理后端和量化版本。
- 本版本必须提供独立的命令行 A/B 验收工具，输出加载时间、纯推理时间、RTF、峰值显存和原始文本。

### 6.3 准确率验收

建立不少于 100 条真实录音的固定测试集，至少包含：

- 中文短句。
- 连续英文。
- 同一句内多次中英文切换。
- 英文技术术语和产品名。
- 数字、日期、金额、单位和缩写。
- 普通话口音、轻声和吞音。
- 安静、键盘声和一般室内噪声。
- 5 秒以内短句和 30 秒以上长句。

记录以下指标：

- 中文字符错误率 CER。
- 英文词错误率 WER。
- 中英文切换边界错误数。
- 专有名词完全正确率。
- 首次临时文本延迟。
- 临时文本回改率。
- 最终识别 RTF。
- 峰值显存和内存。

验收要求：

- 测试集必须保存人工校对的参考文本，并冻结为带版本号的验收数据集。
- 分别报告中文 CER、英文 WER、中英文边界错误数和专有名词完全正确率，不使用含义不明确的单一“综合准确率”。
- 两模型的中文 CER 均不得高于 5%。
- 两模型的英文 WER 均不得高于 10%。
- 两模型的专有名词完全正确率均不得低于 90%。
- 中英文切换边界错误句数不得超过中英混合测试句总数的 5%。
- 1.7B INT8 的中文 CER 和英文 WER均不得高于 0.6B FP16，专有名词完全正确率不得降低。
- VIM 第一版可作为附加对照组记录，但不作为缺少历史基线时的发布前置条件。
- 对两模型输出不一致的样本必须保留音频、参考文本和两份结果，支持人工复核。
- 性能测试必须在相同音频、相同设备和无其他 GPU 工作负载的条件下执行。

### 6.4 可靠性要求

- 连续完成 100 次开始、停止、识别循环，不崩溃、不泄漏模型实例。
- 连续切换模型 20 次，不发生显存持续增长。
- 麦克风断开后给出错误，重新连接后可恢复。
- 模型失败后热键、托盘和录音状态必须恢复一致。
- 单实例运行，重复启动时提示用户。
- 退出程序后不得残留 ASR Worker 或 GPU 显存占用。
- 临时录音文件按最终识别章节定义的成功、放弃或退出条件删除。
- 连续录音期间，音频回调不得报告输入溢出；最终采集帧数与采样率计算出的录音时长和墙钟时长误差不得超过 1%。
- 模型推理期间每 100 毫秒探测一次 UI 消息响应；连续无响应时间不得超过 500 毫秒。
- 自动粘贴至少在 Windows 记事本、Microsoft Word、主流浏览器文本框、Visual Studio Code 和一次远程桌面会话中各连续测试 20 次，成功率必须为 100%。

### 6.5 本版本交付范围

- Portable 目录和离线模型加载。
- 0.6B FP16。
- 1.7B INT8。
- 全局热键录音。
- 同模型实时转写。
- 停止后最终识别。
- 自动粘贴。
- 托盘状态和错误提示。
- 托盘模型切换。
- 命令行 A/B 性能与准确率验收工具。
